import actionTypes from '../actions/actionTypes'
const initialState = {}
 const homeReducer = (state = initialState, action)=>{
    switch (action.type) {
        case actionTypes: return state
        default : return state
    }
}
export default homeReducer
